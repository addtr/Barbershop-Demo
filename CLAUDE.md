@AGENTS.md

# Preferences

- When a task involves a deploy, build, CI run, or anything that takes time to complete in the background, always confirm it actually finished (and succeeded) before telling the user to go check it.
